<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ciscu24.realmeme">

    <application
        android:name=".views.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_realmeme_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_realmeme_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RealMeme">
        <activity
            android:name=".views.SearchActivity"
            android:label="@string/title_activity_search"
            android:theme="@style/Theme.RealMeme.NoActionBar"></activity>
        <activity
            android:name=".views.AboutActivity"
            android:label="@string/title_activity_about"
            android:theme="@style/Theme.RealMeme.NoActionBar" />
        <activity
            android:name=".views.FormActivity"
            android:label="@string/title_activity_form"
            android:theme="@style/Theme.RealMeme.NoActionBar" />
        <activity
            android:name=".views.LogoActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.ListActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.RealMeme.NoActionBar" />
    </application>

</manifest>